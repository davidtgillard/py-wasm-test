package example:calc;

world adder {
    record add-result {
        length: u32,
        data: list<u8>,
    }
    variant add-result-or-error {
        ok(add-result),
        err(string),
    }
    export add: func(data: list<u8>) -> add-result-or-error;
}
